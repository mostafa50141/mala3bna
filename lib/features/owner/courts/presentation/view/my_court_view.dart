import 'package:flutter/material.dart';

class MyCourtView extends StatelessWidget {
  const MyCourtView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("My Courts View"));
  }
}
