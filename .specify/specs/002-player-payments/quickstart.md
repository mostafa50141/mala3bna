# Quickstart: Player Payments Screen

This feature operates via hardcoded static mock lists in the `PaymentsBody` directly rendering `PaymentsView`.

1. Launch application in emulator. 
```bash
flutter run
```
2. Navigate to Profile Tab using bottom navigation menu.
3. Tap the "Payments" ProfileMenuItem which contains route mapping configured.
